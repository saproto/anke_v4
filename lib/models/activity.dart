class Activity{
  final int id;
  final String title;
  final String description;
  final DateTime startDate;
  final DateTime endDate;
  final String location;

  Activity(this.id, this.title, this.description, this.startDate, this.endDate, this.location); //this.id, this.title, this.description, this.startDate, this.endDate, this.location);
}


